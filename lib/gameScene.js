var scene = {
    get frameHeight () {
        return canvas.frameHeight;
    },
    get frameWidth () {
        return canvas.frameWidth;
    }
};

module.exports = {
    initialize: function (scn) {
        
    }
}
