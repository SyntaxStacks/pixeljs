var gameInput = require('../../lib/gameInput');
describe('gameInput', function () {
  var sandbox;

  beforeEach(function () {
    sandbox = sinon.sandbox.create();
  });

  afterEach(function () {
    sandbox.restore();
  });

  it('should get inputs');
  it('should add input');

});
