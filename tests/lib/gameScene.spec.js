var gameInput = require('../../lib/gameInput');
describe('gameInput', function () {
  var sandbox;

  beforeEach(function () {
    sandbox = sinon.sandbox.create();
  });

  afterEach(function () {
    sandbox.restore();
  });

  it('should get initialize scene');

});
